{
  "name": "qoyod-creative-os",
  "version": "1.0.0",
  "private": true,
  "description": "Qoyod Creative OS — internal AI-powered marketing tool for the Qoyod team",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c cyan,magenta \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "console": "npm --prefix server run console",
    "asana:register": "npm --prefix server run asana:register",
    "slack:test": "npm --prefix server run slack:test",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "start": "npm --prefix server run start",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
